import type { ModuleRunner } from '../orchestrator/types.js';

/**
 * Plantilla para la vista general de futuros. Cuando se complete la
 * integración deberá cubrir la navegación al layout de *overview*, habilitar el
 * sniffer y serializar los resúmenes diarios de cada contrato seguido.
 */
export const runFuturesOverviewModule: ModuleRunner = async () => {
  return undefined;
};
